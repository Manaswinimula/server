{
  "name": "frontlinedesign-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
